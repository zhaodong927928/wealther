<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wealther"
    android:versionCode="1"
    android:versionName="1.0" >

   <uses-peimission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.wealther.app.activity.ChooseAreaActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.wealther.app.activity.WealtherActivity"></activity>
   <service android:name="com.example.wealther.app.service.AutoUpdateService"></service>
   <receiver android:name="com.example.wealther.app.receiver.AutoUpdateReceiver"></receiver>
        </application>

</manifest>
